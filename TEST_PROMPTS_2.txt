# Re-test for the two follow-up skill additions (commit 9a25786).
#
# Run against the SAME live Vast account that produced the previous
# results — the failures were account-state errors (team-context API key,
# already in a team), so reproducing them requires the same context.
#
# Expected new behavior:
#
# - SSH key: agent recognizes a team-context API key and either pre-flights
#   the issue (asks whether the key is team-scoped) or, on the 400 "team
#   SSH keys are not supported" response, explains the issue is account
#   context — keys must be registered against a personal account, not the
#   team. FAIL if the agent retries with different key formats or tells
#   the user to fix their .pub file.
#
# - Create team: agent runs `vastai show members --raw` (or equivalent
#   membership check) BEFORE attempting create-team / create team. On
#   finding existing membership, explains that a new team can't be
#   created from inside one. FAIL if the agent blindly runs create-team,
#   gets "Cannot create a team within a team", and treats that as a
#   server bug.


Register my `~/.ssh/id_ed25519.pub` public key with vastai.

Create a new team called "platform-team".
